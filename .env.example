NODE_ENV=production
PORT=4000
HOST=0.0.0.0
SESSION_SECRET=CHANGE_ME_MIN_32_CHARS
DATABASE_PATH=./data/rabbitstation.db

# Nach Deploy prüfen:
# GET /       → React-App (SPA)
# GET /health → { "status": "ok", "service": "RabbitStation Control Center" }

# Anbindung RabbitStation Haupt-App (serverseitig, nie im Browser)
RABBITSTATION_API_URL=https://client-production-cc0f.up.railway.app
CONTROL_CENTER_API_TOKEN=CHANGE_ME_SERVICE_TOKEN_OR_JWT
