{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        { "src": "package.json", "dest": "package.json" },
        { "src": "package-lock.json", "dest": "package-lock.json" },
        {
          "cmd": "npm ci --include=dev --no-audit --no-fund",
          "customName": "Install dependencies (incl. dev)"
        }
      ]
    }
  }
}
