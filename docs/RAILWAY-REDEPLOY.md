# Railway Redeploy (Control Center)

Siehe auch Haupt-App: gleicher Ablauf für beide Repos nach Railway-Ausfall.

```powershell
cd "C:\Users\rabbi\Documents\Projekt Verkauf WEBAPP"
git pull origin main
git commit --allow-empty -m "chore: Railway-Redeploy ausloesen"
git push origin main
```
